# Copy to .env and edit. Used by docker-compose.
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=change-me
# External URL Grafana is served on (via Caddy). Match your Caddy site, e.g.:
GF_ROOT_URL=https://solar.bodka
